import 'package:flutter/material.dart';

class LikedYouScreen extends StatelessWidget {
  const LikedYouScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Column(
          children: [Text("Liked You Screen")],
        ),
      ),
    );
  }
}
